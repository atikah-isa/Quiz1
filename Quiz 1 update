class Square:
    side = 1.0
    
    def setSide(self, side):
        self.side = side*4
        
        if self.side<0:
                self.side = 0
    
    def display(self):
        print ("Parameter: ",self.side)
        
side1= Square()
side1.setSide(3.0)
side1.display()
